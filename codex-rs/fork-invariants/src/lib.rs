//! Behavioural invariants of the ore fork.
//!
//! The library is intentionally empty; every invariant lives in `tests/` so
//! that nothing here can end up linked into a shipped binary. Tree-, git- and
//! artifact-level invariants live outside the workspace in `fork/verify/`.
