# ore publishes no announcements. The TUI fetches this file from the repo on
# every start, and an empty list is the valid "nothing to say" answer; deleting
# the file would leave the fetch 404-ing forever.
announcements = []
