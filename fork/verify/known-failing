# fork/verify/known-failing — upstream tests that legitimately go red under the
# fork's seams (series commits telemetry-analytics-off, telemetry-feedback-off,
# telemetry-statsig-strip, telemetry-defaults-none).
#
# Format: one nextest filterset expression per line ('#' comments, blank lines
# ignored).  Consumers OR the expressions together, e.g. to exclude them from
# the required-green set:
#   cargo nextest run -E "not ($(grep -v '^#' known-failing | grep . | paste -sd'|' - | sed 's/|/ or /g'))"
#
# Provenance: the telemetry-seam verification pass, then re-derived
# test-by-test against
# the rust-v0.149.0 tree by the seam commit itself, which seeded this file;
# each later series commit adds its own cause group.
# Three causes:
#   (1) analytics force-off — `Config.analytics_enabled` is `Some(false)` for
#       every config layer, so `AnalyticsEventsClient` never builds a queue and
#       every test awaiting a POST to /codex/analytics-events/events times out;
#   (2) accepted Caveat A — `otel_init.rs` nulls the WHOLE metrics exporter
#       when analytics is off, user BYO OTLP collectors included, so tests that
#       await their own /metrics endpoint time out;
#   (3) feedback force-off — `Config.feedback_enabled` is `false`, so the
#       `/feedback` TUI command and the `feedback/upload` RPC report disabled.
#
# NOT in this list, deliberately (verified to keep passing):
# * mcp-server lib.rs default-analytics assertions and app-server
#   v2/analytics.rs :56/:81 — they assert an untouched constant or reset
#   `analytics_enabled` themselves after load;
# * cli clap flag tests — parsing only;
# * otel crate unit/suite tests — they build `OtelSettings` directly with
#   explicit exporters, and `resolve_exporter(Statsig)` already returned None
#   under debug_assertions before the strip;
# * app-server v2/remote_control.rs — passes `default_analytics_enabled` to the
#   spawn helper but never awaits an analytics POST;
# * app-server v2/turn_steer.rs `turn_steer_rejects_oversized_text_input` and
#   `turn_steer_rejects_context_only_input_without_merging_context` — they
#   mount the analytics capture server but never await it;
# * core config_tests.rs `metrics_exporter_defaults_to_*` — the seam commit
#   flips that expectation in place (the one upstream test it edits);
# * app-server v2/curated_mcp_sync.rs
#   `existing_thread_loads_api_curated_mcp_after_auth_switch_sync` — its
#   fixture opts back in via ORE_CURATED_PLUGINS_REPO, so the sync clones the
#   local git fixture exactly as upstream's hard-coded repo did.

# --- (3) feedback force-off -------------------------------------------------
# Passes `-c analytics.enabled=true` and expects three or more POSTs to
# /events; the seam pins analytics_enabled to Some(false), so no queue is built
# and nothing is ever sent. This is the behaviour codex-fork-invariants asserts.
package(codex-exec) & test(shutdown_flushes_completed_turn_and_file_diff)
# Asserts `feedback_enabled == true` for a default `[feedback]` table.
package(codex-core) & test(feedback_enabled_defaults_to_true)
# Drives feedback/upload through a failing proxy and asserts a -32603 transport
# error; the config gate now rejects it first with "disabled by configuration".
package(codex-app-server) & test(feedback_upload_reports_transport_failure_as_json_rpc_error)
# Snapshot of the /feedback category popup; the disabled notice renders instead.
package(codex-tui) & test(feedback_selection_popup_snapshot)

# --- (2) user BYO OTLP metrics (accepted Caveat A) --------------------------
# Both configure `[otel] metrics_exporter = { otlp-http = ... }` against a local
# collector and assert a flush on shutdown. The analytics seam gates metrics
# regardless of which exporter is configured, so a user's own endpoint receives
# nothing -- which is Caveat A exactly, ratified with decision 3.
package(codex-cli) & test(local_exec_server_flushes_telemetry_on_stdio_disconnect)
package(codex-cli) & test(remote_exec_server_gracefully_stops_when_parent_stdin_closes)
# Asserts codex.thread.started reaches a user otlp-http collector.
package(codex-app-server) & test(account_switch_reloads_telemetry_collectors_and_preserves_trace_context)
# guardian_v2: every test routes through guardian_v2_routes_tool_approvals,
# which awaits a codex.guardian_v2.classification metric on its own /metrics
# endpoint (suite/v2/guardian_v2.rs:354).
package(codex-app-server) & test(guardian_v2_low_risk_actions_skip_subsequent_reviews)
package(codex-app-server) & test(guardian_v2_high_risk_actions_require_full_reviews)
package(codex-app-server) & test(guardian_v2_threshold_score_requires_full_reviews)
# Two more guardian_v2 entries and one plugin-measurement entry were dropped at
# rust-v0.150.1: upstream had renamed or removed those tests, so the expressions
# matched nothing and had been excluding nothing. known_failing_check.py exists
# to catch that from now on.
#
# Two of the three came back at rust-v0.151.0 under new names -- upstream had
# renamed, not removed them:
#   {resumed,forked}_thread_starts_without_guardian_score
#     -> {resumed,forked}_thread_ignores_persisted_guardian_score
# Same file, same helper, same /metrics timeout, so the cause is unchanged and
# they belong here rather than in a new group. The plugin-measurement entry is
# still gone.
package(codex-app-server) & test(resumed_thread_ignores_persisted_guardian_score)
package(codex-app-server) & test(forked_thread_ignores_persisted_guardian_score)
# Moved here from cause (4) after ore-upstream-probe ran them on a pristine
# rust-v0.152.0 on ubuntu-24.04: all seven cases passed, so upstream is not the
# reason. They route through the same helper as everything else in this group and
# reach the same classification_completed await, which is what the mechanism said
# from the start -- the macOS stack overflow is what mis-filed them.
package(codex-app-server) & test(guardian_v2_computer_use_only_scopes_classification_and_fast_reviews)
package(codex-app-server) & test(guardian_v2_discards_sync_reviews_after_authorization_changes)

# --- (1) codex-app-server: tests awaiting an analytics POST -----------------
# suite/v2/analytics.rs — the five plugin-measurement tests share
# assert_plugin_measurement_analytics.
package(codex-app-server) & test(unified_plugin_script_emits_measurement_analytics)
package(codex-app-server) & test(remote_unified_plugin_script_emits_measurement_analytics)
package(codex-app-server) & test(unified_background_plugin_script_emits_measurements_after_turn_completion)
package(codex-app-server) & test(remote_unified_background_plugin_script_emits_measurements_after_turn_completion)
# suite/v2/thread_start.rs, thread_resume.rs, thread_fork.rs, thread_archive.rs
package(codex-app-server) & test(thread_start_tracks_thread_initialized_analytics)
package(codex-app-server) & test(thread_resume_tracks_thread_initialized_analytics)
package(codex-app-server) & test(thread_resume_running_thread_tracks_thread_originator_in_analytics)
package(codex-app-server) & test(thread_goal_lifecycle_emits_analytics_and_clear_deletes_goal)
package(codex-app-server) & test(thread_fork_tracks_thread_initialized_analytics)
package(codex-app-server) & test(thread_archive_succeeds_when_descendant_archive_fails)
# suite/v2/turn_start.rs, turn_steer.rs
package(codex-app-server) & test(turn_start_tracks_thread_originator_in_analytics)
package(codex-app-server) & test(code_mode_exec_emits_correlated_production_analytics)
package(codex-app-server) & test(turn_profile_tracks_blocking_tool_and_follow_up_sampling)
package(codex-app-server) & test(turn_steer_requires_active_turn)
package(codex-app-server) & test(turn_steer_returns_active_turn_id)
# suite/v2/external_agent_config.rs
package(codex-app-server) & test(external_agent_config_import_completed_tracks_analytics_event)
package(codex-app-server) & test(external_agent_config_import_reports_session_config_error_subtype)
package(codex-app-server) & test(external_agent_config_import_reinstalls_plugins_from_known_marketplaces)
# Polls the analytics capture file in a loop for
# codex_onboarding_external_agent_import_complete rather than going through
# the shared await helper, so it spins to nextest's 60s timeout instead of
# failing an assertion. Same cause as its three siblings above.
package(codex-app-server) & test(external_agent_config_import_reports_failed_sync_import_in_completion)
# suite/v2/executor_skills.rs — only the ExplicitOnly scenario awaits analytics.
package(codex-app-server) & test(explicit_executor_skill_can_read_referenced_file)
# suite/v2/plugin_install.rs
package(codex-app-server) & test(plugin_install_tracks_analytics_when_remote_detail_fetch_fails)
package(codex-app-server) & test(plugin_install_tracks_analytics_when_remote_install_is_rate_limited)
package(codex-app-server) & test(plugin_install_rejects_remote_plugin_disabled_by_admin_before_download)
package(codex-app-server) & test(plugin_install_rejects_remote_plugin_not_available)
package(codex-app-server) & test(plugin_install_tracks_analytics_when_marketplace_file_cannot_be_read)
package(codex-app-server) & test(plugin_install_tracks_analytics_event)
package(codex-app-server) & test(plugin_install_failure_tracks_analytics_event)
package(codex-app-server) & test(plugin_install_tracks_remote_plugin_analytics_event)
package(codex-app-server) & test(plugin_install_preserves_status_when_remote_bundle_error_body_is_too_large)
# suite/v2/plugin_uninstall.rs
package(codex-app-server) & test(plugin_uninstall_tracks_analytics_event)
package(codex-app-server) & test(plugin_uninstall_writes_remote_plugin_to_cloud_when_remote_plugin_enabled)

# --- (1) codex-core: integration tests awaiting analytics events ------------
# tests/suite/plugins.rs :472, :1352, :1401, :1480 (the first and last are
# #[test_case]-parameterised; the substring filter covers every variant).
package(codex-core) & test(persisted_remote_plugin_command_attribution_flows_through_turn_context)
package(codex-core) & test(explicit_plugin_mentions_track_plugin_used_analytics)
package(codex-core) & test(explicit_plugin_skill_invocation_tracks_remote_plugin_id)
package(codex-core) & test(implicit_plugin_skill_invocation_tracks_remote_plugin_id)
# tests/suite/skills_extension.rs — wait_for_analytics_events at :1037, :1062,
# :1337, :1873.
package(codex-core) & test(explicit_only_orchestrator_skill_is_hidden_but_can_be_invoked)
package(codex-core) & test(executor_skill_invocation_is_environment_scoped_and_deduplicated)
package(codex-core) & test(production_turn_uses_provider_host_catalog_and_core_snapshot_injection)
# tests/suite/request_plugin_install.rs :840 — the wait lives in the shared
# helper run_remote_plugin_install_metadata_case, whose only caller is:
package(codex-core) & test(endpoint_recommendation_hydrates_install_identity_after_selection)

# ---------------------------------------------------------------- rust-v0.150.1
# Upstream reworked the turn path at rust-v0.150.1, and more of its app-server
# suite now waits on the analytics POST that cause (1) makes unreachable. Each
# entry below was demonstrated, not assumed: it passes at the commit BEFORE
# telemetry-analytics-off (9d82eaf5) and times out at the series tip, and
# `git bisect run` over the whole series named 3af82650cd -- "core: force
# analytics off at the config seam" -- as the first bad commit. All of them also
# pass on a pristine upstream rust-v0.150.1 checkout, so nothing about the tests
# themselves is broken.
#
# Symptom is uniform: `Error: deadline has elapsed` after turn/started and
# item/started, at ~15s, or ~35s for the two that name analytics outright.
package(codex-app-server) & test(turn_start_with_empty_input_runs_model_request)
package(codex-app-server) & test(direct_input_to_multi_agent_v2_subagent_is_rejected)
package(codex-app-server) & test(turn_start_emits_spawn_agent_item_with_model_metadata_v2)
package(codex-app-server) & test(thread_archive_requires_materialized_rollout)
package(codex-app-server) & test(history_notes_and_async_message_emit_control_tool_analytics)
package(codex-app-server) & test(multi_agent_v2_tools_emit_collaborator_analytics)

# (4) NOT ours — red on pristine upstream at the same tag.
#
# A fourth cause, and the first entries here that are not about a fork seam, so
# they are kept separate rather than filed under (1). Both were run against an
# unmodified rust-v0.150.1 worktree, same machine, same prebuilt v8, and failed
# identically there. Excluding them keeps ore's CI reporting on ore's tree; it
# does not paper over a fork regression, because there is no fork involvement to
# paper over.
#
# Re-check these on the next sync: an upstream fix should let them come out.
#
# Re-checked at rust-v0.151.0. The mcp-server entry is still upstream-red:
# test_exec_command_approval_triggers_elicitation fails on a pristine
# rust-v0.151.0 checkout in 0.09s, with no guardian and no metrics involvement,
# so that result is clean and the entry stays.
#
# The two guardian_v2 entries below were NOT re-checked, deliberately. A pristine
# rust-v0.151.0 run of the guardian family on macOS dies of
#   thread 'codex-approval-review' has overflowed its stack
# which appears nowhere in ore's Linux CI, so a macOS re-check cannot separate an
# upstream regression from that local fault. They need a Linux runner -- the same
# limit the rust-v0.151.0 guardian block below records for its own entries.
# RE-FILED, and the two guardian entries that were here are gone from cause (4):
# ore-upstream-probe ran them against a pristine rust-v0.152.0 on ubuntu-24.04 and
# all seven cases PASSED. They were never upstream-red. The macOS evidence that
# put them here was the approval-review stack overflow, which does not happen on
# Linux -- the same false signal that made the rust-v0.151.0 block file 16 cases
# from mechanism alone. They now sit under cause (2) below, where the mechanism
# always said they belonged.
#
# test_exec_command_approval_triggers_elicitation stays: it FAILS on pristine
# rust-v0.152.0 on Linux, in 0.13s, an assertion rather than a signal.
package(codex-mcp-server) & test(test_exec_command_approval_triggers_elicitation)
# Overflows the stack in a debug build, on an unmodified rust-v0.150.1 worktree
# as readily as on ore's. Not an assertion failure -- `fatal runtime error: stack
# overflow` before any assertion runs. Confirmed on LINUX at rust-v0.152.0 by
# ore-upstream-probe, which reports it as ABRT: this one really is upstream's.
package(codex-tui) & test(active_patch_approval_pager_preserves_changes_and_accepts_once)
# New at rust-v0.150.1, so it has no passing baseline here. Its sandbox mounts the
# test's temp dir read-only and the shell profile then fails to write into it:
#   /tmp/.../.bashrc: line 1: /tmp/.../captures: Read-only file system
# exiting 41 where the test wants 0. That is bubblewrap's filesystem behaviour on
# the runner, and ore changes nothing about sandboxing -- its series is telemetry,
# the config home, the providers and the rename. It fails all three attempts, so
# it is deterministic rather than contended, and it passes on macOS for ore and
# for a pristine rust-v0.150.1 checkout alike.
#
# Widened from the single test to the shell_snapshot_v2 family: its siblings fail
# the same way for the same reason, and naming them one at a time only meant a
# further CI round to find the next one.
#
# The HONEST LIMIT this entry carried -- "could NOT be checked against pristine
# upstream on Linux" -- is discharged, and the answer inverts the entry. Every
# codex-exec-server shell_snapshot_v2 case PASSES on a pristine rust-v0.152.0 on
# ubuntu-24.04. So it is not upstream-red, and if it is red on ore it is ore's.
#
# Kept excluded rather than deleted, because what makes it fail on ore has not
# been established -- only that upstream is not the reason. The bubblewrap
# read-only-mount theory above was inferred from ore's own runner and never
# tested against upstream's. NEXT STEP: run it on ore's Linux CI unexcluded and
# read the actual failure; do not carry the old explanation forward.
package(codex-exec-server) & test(shell_snapshot_v2_)

# Fails on a pristine rust-v0.150.1 checkout as readily as here, and also fails
# BEFORE the analytics seam, so it is neither cause (1) nor ours. Symptom is
# `timeout waiting for event: Elapsed(())`. A sibling in the same file is already
# excluded above for cause (1); this one is not that, despite the resemblance.
package(codex-core) & test(snapshot_rollback_followup_turn_trims_context_updates)

# ---------------------------------------------------------------- rust-v0.151.0

# --- (1) analytics force-off, one test that used to pass --------------------
# The only entry here that regressed rather than arrived: this test existed at
# rust-v0.150.1 and passed on ore, because it did not touch analytics. At
# rust-v0.151.0 upstream added an imagegen analytics event and made the test
# await it:
#     +use super::analytics::wait_for_analytics_event;
#     +let event = wait_for_analytics_event(&server, DEFAULT_READ_TIMEOUT,
#     +    "codex_image_generation_event").await?;
# so it joined cause (1) the moment the base advanced. Demonstrated rather than
# inferred from resemblance: the diff above is what introduced the wait, the test
# passes on a pristine rust-v0.151.0 checkout (1 passed, 1100 skipped), and it
# fails all three attempts on the candidate.
package(codex-app-server) & test(standalone_image_generation_returns_saved_path_hint_to_model)

# --- (2) user BYO OTLP metrics (accepted Caveat A) --------------------------
# New at rust-v0.151.0. Both write `[analytics] enabled = true` AND their own
# `[otel.metrics_exporter.otlp-http] endpoint = <local MockServer>` into
# config.toml, then await an export on that endpoint -- the http one on
# /v1/metrics, the grpc one on the OTLP MetricsService/Export path. Caveat A
# exactly: the seam nulls the metrics exporter whenever analytics is off, so the
# `enabled = true` in their own fixture cannot reach it. The tests say so
# themselves -- they fail with
#     Error: collector never received a SQLite log-write metric
# which is Caveat A stated in the test's own words. All three cases pass on a
# pristine rust-v0.151.0 checkout (3 passed, 1098 skipped), so the seam is the
# whole difference. Their config block, that message and that run are what put
# them here, not the resemblance to the entries above.
package(codex-app-server) & test(sqlite_log_metrics_exports_do_not_create_log_cycles)
package(codex-app-server) & test(sqlite_log_metrics_grpc_exports_do_not_create_log_cycles)

# --- (2) guardian_v2, the rest of the family that arrived at rust-v0.151.0 ---
# Upstream added six guardian_v2 tests at this tag, 26 parameterised cases in
# all, and every one of them lands on the mechanism the three cause (2) entries
# above already name -- now literally in the same function:
# guardian_v2_routes_tool_approvals is a thin wrapper over
# guardian_v2_routes_scoped_tool_approvals, whose body holds
#
#     if classifier_in_scope {
#         ...
#         timeout(TIMEOUT, responses_state.classification_completed.notified()).await?;
#
# and classification_completed is notified from the test's OWN /metrics route,
# only when the exported body contains `codex.guardian_v2.classification`. The
# seam nulls the metrics exporter, so that notify never fires and the timeout is
# reached. Symptom on ore's CI matches exactly: `Error: deadline has elapsed`,
# every case, all three attempts.
#
# The `not test(other_tools_retain_full_review)` below is the evidence, not a
# convenience. That case is the ONE variant of its test where
# classifier_in_scope is false -- it passes TEST_SERVER_NAME rather than a
# node_repl-backed server -- so it never reaches the await, and it is the ONE
# case of the 27 that ore's CI passes. The mechanism predicts which cases fail
# and which do not, and it is right about all of them. Keeping it out of this
# expression keeps it in the required-green set, where it belongs.
#
# SETTLED. This was filed from the mechanism because a pristine rust-v0.151.0 run
# on macOS passed 10 of the 26 and failed 16, and those 16 died there of
#   thread 'codex-approval-review' has overflowed its stack
# which appears nowhere in ore's Linux CI -- a second, platform-local fault that
# made the comparison unusable. ore-upstream-probe answered it on the platform it
# had to be answered on:
#
#   Summary [157.949s] 16 tests run: 16 passed, 1400 skipped
#
# pristine rust-v0.152.0, ubuntu-24.04. None of the 16 is upstream-red; all of
# them are the seam, exactly as classifier_in_scope predicted. The mechanism was
# right and the macOS run was noise.
#
# The wider lesson is recorded here because it cost three mis-filed entries: a
# macOS verdict about upstream is not evidence about ore's Linux CI. Every
# cause (4) claim should be re-derived with ore-upstream-probe before it is
# trusted, and the ones above have been.
package(codex-app-server) & test(guardian_v2_discards_sync_reviews_after_user_input_answer)
package(codex-app-server) & test(guardian_v2_inherits_root_user_skills_for_delegated_workers)
package(codex-app-server) & test(guardian_v2_low_scores_require_current_authorization)
package(codex-app-server) & test(guardian_v2_propagates_root_user_input_to_worker_reviews)
package(codex-app-server) & test(guardian_v2_validates_user_input_before_history_truncation)
package(codex-app-server) & test(guardian_v2_required_model_computer_use_preserves_strict_approval) & not test(other_tools_retain_full_review)

# --- (1) two that time out rather than assert ------------------------------
# Found late, and worth saying how: the first triage of this sync extracted
# `TRY 3 FAIL` from the CI log and reported 31 failures. nextest also reports
# `TRY 3 TMT` for a test killed at the slow-timeout ceiling, and these two were
# in that category all along -- 33, not 31. A grep that silently drops a whole
# outcome class is the same silent-green shape this file exists to prevent, one
# level up.
#
# Both await an analytics POST that cause (1) makes unreachable, and both spin
# to nextest's 60s ceiling instead of failing an assertion, because the wait is
# the last thing they do:
#   client_metadata.rs -- wait_for_analytics_event(&server, .., "codex_turn_event")
#   analytics.rs       -- wait_for_matching_analytics_event(&server, .., |event|
#                           event["event_type"] == "codex_turn_event" && ..)
#
# turn_start_sends_nested_subagent_lineage_after_cold_thread_resume_v2 is
# demonstrated: it passes on a pristine rust-v0.151.0 checkout in 0.194s and
# times out here on all three attempts.
#
# guardian_review_turns_and_tools_reach_analytics is filed from the source and
# from ore's own CI. Its pristine-upstream run on macOS is not usable evidence:
# it dies there of the same `codex-approval-review` stack overflow described in
# the guardian block above, which appears nowhere in ore's Linux CI. Same honest
# limit, same re-check owed on a Linux runner.
package(codex-app-server) & test(turn_start_sends_nested_subagent_lineage_after_cold_thread_resume_v2)
package(codex-app-server) & test(guardian_review_turns_and_tools_reach_analytics)

# ---------------------------------------------------------------- rust-v0.152.0
# Both new at this tag, and both found by `fork/preflight.sh --tests` before CI
# ran -- which is what that pass was added for after rust-v0.151.0 spent three CI
# rounds discovering entries one batch at a time.

# --- (1) analytics force-off ------------------------------------------------
# suite/v2/sleep.rs ends on
#   wait_for_matching_analytics_event(&server, DEFAULT_READ_TIMEOUT, |event| ..)
# so it waits for a POST the seam makes unreachable. Same shape as
# guardian_review_turns_and_tools_reach_analytics above, and it fails the same
# way: the wait is the last thing the test does, so it reaches the ceiling rather
# than failing an assertion.
package(codex-app-server) & test(clock_tools_emit_control_tool_analytics)

# --- (2) user BYO OTLP metrics (accepted Caveat A) --------------------------
# Routes through guardian_v2_routes_tool_approvals with
# ModelReviewRequirement::Optional, and that wrapper passes
# GuardianToolScope::AllTools -- for which classifier_in_scope is
# `matches!(requirement, Optional)`, so it is true and the test reaches the
# `classification_completed` await on its own /metrics endpoint. That is the same
# predicate recorded in the rust-v0.151.0 guardian block, which correctly
# predicted both which cases fail and which single case does not.
package(codex-app-server) & test(guardian_v2_required_model_cannot_reuse_a_cached_score_for_skipped_exec)
