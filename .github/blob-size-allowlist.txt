# Paths are matched exactly, relative to the repository root.
# Keep this list short and limited to intentional large checked-in assets.

.github/codex-cli-splash.png
# ore's README header. 965 KB of ASCII-art animation: reducing the palette to fit
# the 500 KB limit made the word "ore" indistinguishable, which is the one thing
# the asset exists to show. An exception for a deliberate identity asset is what
# this list is for -- upstream's own splash is on it directly above.
.github/ore-splash.gif
MODULE.bazel.lock
codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.schemas.json
codex-rs/app-server-protocol/schema/json/codex_app_server_protocol.v2.schemas.json
codex-rs/tui/tests/fixtures/oss-story.jsonl
codex-rs/tui_app_server/tests/fixtures/oss-story.jsonl
codex-rs/tui/src/app.rs
